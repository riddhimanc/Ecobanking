
spring:
  application:
    name: core

  datasource:
    url: jdbc:h2:mem:ecobank;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: update
      defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate.format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  ai:
    openai:
      api-key: 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   # DO NOT hard-code keys; use env var
      audio:
        speech:
          enabled: false           # disable speech auto-config (not needed now)

  security:
    oauth2:
      client:
        registration:
          google:
            client-id:   "218700452912-vedrd1t3g154p52t4qij7pdj7ctbcaqn.apps.googleusercontent.com"
            client-secret: "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"
            scope:
              - openid
              - email
              - profile

server:
  port: 8080

# For local dev only—open CORS to React app
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000,http://localhost:4200
